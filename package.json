{
    "name": "@dotrly/helper",
    "version": "0.1.0",
    "description": "Public logic and commands for the Relay Helper.",
    "type": "module",
    "main": "./dist/index.js",
    "scripts": {
        "dev": "tsup src/index.ts --format esm --watch",
        "build": "tsup src/index.ts --format esm --clean --minify"
    },
    "dependencies": {
        "@dotrly/sdk": "^0.2.1"
    },
    "devDependencies": {
        "tsup": "^8.5.1",
        "typescript": "^5.9.3"
    }
}
